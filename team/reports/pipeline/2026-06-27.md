# Pipeline Health — 2026-06-27 — YELLOW

Thirteenth audit. Tenth consecutive silent day. Today is the day the team's own
nine-day-old pre-announced wall fired — the most consequential mechanical event of the
cycle so far. The process held up under stress-testing better than I expected going in.
It is not flawless: a real arithmetic regression landed in two artifacts today, on the
exact day precision matters most, and I'm not waving that through because the bigger
call was right.

## 1. Tenth-consecutive-silent-day claim — verified independently

Read directly, not taken on Hemera's or Nyx's word:
- `team/state/cursors.json` — `discord: {}`, `imap.lastUid: 0`, `copyparty.seenShas: []`.
  All empty.
- `team/inbox/` — globbed directly: only `team/inbox/audio/.gitkeep` exists. Zero intake
  records.
- `team/approvals/queue.json` — `updatedAt` is `2026-06-27T09:00:00Z` today (Hemera wrote
  to it), but the three pre-existing items' own `createdAt` fields are untouched
  (`2026-06-12T12:30:00Z`, `2026-06-17T09:00:00Z`, `2026-06-18T11:00:00Z`) and all three
  are still `pending`. No user action landed on any of them.

Silence confirmed, same as every prior day this cycle.

## 2. The load-bearing call: does the fallback fire today, or defer to June 28?

This is the call the whole audit turns on, so I read the primary texts myself rather than
trusting the summary.

**Nightshade** (`t-nightshade-eta-decision`, `q3-2026-release-calendar.md` June 22
section): pre-announced 2026-06-15 — "if no re-mix ETA surfaces by June 22, drop
Nightshade from Q3." Fired on June 22 itself, the literal due date, with zero deferral
clause anywhere in its history.

**Homerun** (`t-homerun-video-decision`, calendar's June 19/20 section): due June 19.
Hemera's June 18/19 turns contain an explicit, written, in-advance deferral clause: "if
silence continues past today's EOD, tomorrow's standup becomes the real checkpoint and
the fallback will be treated as in effect unless told otherwise before then." That clause
is what moved Homerun's activation from June 19 to June 20 — not an inherent property of
"due dates defer to the day after," but a specific instrument written specifically for
Homerun, on the record, before the deadline arrived.

**Switch's June 27 line**: I read the calendar's "Next Check-in Points" section and the
standing policy's part 5 directly. The literal text: "June 27: hard line for Switch
inputs + approval before July 18 needs re-planning. If unmet, the release moves to July
25." Part 5 (added 2026-06-24) names June 26 as the tone-sharpening checkpoint and June 27
as "the day the date actually moves" — explicitly distinguishing the two. I searched the
calendar's full history (June 16 origin through today) for anything resembling a
Homerun-style EOD-deferral clause attached to the June 27 line. There is none. No turn
between June 16 and June 26 contains language like "if June 27 is silent, June 28 becomes
the checkpoint."

**Verdict on the distinction: it holds.** Hemera's reasoning is not assumed analogy, it's
a presence/absence check on a specific textual instrument, and I confirm the instrument is
absent for Switch. I tried the obvious counter-reading — "doesn't a checkpoint always
mean end-of-day, with the consequence assessed the following morning, the way Homerun's
did?" — and it doesn't survive contact with the text: Homerun's deferral was an explicit
exception Hemera wrote in because the alternative (firing same-day, mid-business-hours)
was the default she was deviating from. The fact that she had to write it in at all is
evidence the default is same-day, not day-after. Switch's line never got that exception
written. The fallback firing today is the correct, conservative reading, not a convenient
one.

One adjacent question worth naming for the record, not as a flaw: Nightshade and Homerun
are also genuinely different kinds of fallback (a tentative slot being dropped vs. a path
choice on a locked release) — I made the team correct an over-claimed parallel between
those two back on 2026-06-22/23. Today's Nightshade-vs-Switch comparison is a narrower,
correctly-scoped one (it's only about the deferral-clause mechanic, not about the
fallbacks' substance or stakes), and Hemera doesn't overreach it into a broader claim of
equivalence this time. That's the right lesson taken from the earlier correction.

## 3. Artifact consistency sweep — calendar, checklist.md, overlay.json

**Internal consistency**: all three documents tell the same story in the same order
(silence verified → literal-text reasoning → cost stated in calendar's own numbers →
reversibility stated → queue handled). No contradictions between them.

**Stale July 18 references**: I read all three files in full. Calendar — clean, every
live reference now reads July 25 with the July 18 history preserved as struck-through
record, not live claim. `checklist.md` — clean, target line, all platform schedule lines,
and the sign-off log all read July 25; the `package drafted: 2026-06-12` line is correctly
left alone (historical, not a publish-date claim). `overlay.json`'s new note and refreshed
reminder — clean, no stale July 18.

**Arithmetic — Release Spacing recalculation (21 → 14 days), checked against the
calendar's own numbers, not trusted on the subtraction being reported correctly:** the
calendar states Switch's original target was July 18 and Homerun's is August 8, unchanged.
August 8 minus July 18 = 21 days — matches the original stated spacing. August 8 minus
July 25 = 14 days — matches today's claimed recalculation exactly. The arithmetic is
correct.

**Arithmetic — queue ages, the actual problem I found.** The calendar's 2026-06-27 log
entry states: "recomputed ages from raw `createdAt`: `ap-2026-06-19-switch-upload` 8 days
since June 19, `ap-2026-06-17-switch-w25-collab-story` 10 days since June 17,
`ap-2026-06-18-switch-w26-x-post` 9 days since June 18." I checked this against
`queue.json` directly. The collab-story and X-post numbers are right (June 17→27 = 10
days, June 18→27 = 9 days). The upload-package number is wrong, and it's wrong in a way
that breaks a pattern this team has gotten right for five straight days: that item's real
`createdAt` is `2026-06-12T12:30:00Z`, not June 19 — June 19 is only the date embedded in
its id slug (`ap-2026-06-19-switch-upload`), assigned because that's when it was queued
for the approval gate, not when it was created. Every prior audit (06-23 through 06-26,
and my own 06-26 report) anchored this item's age correctly to June 12 and got 11, 12, 13,
14 days on successive days. Today's entry silently switched the anchor to the id-slug date
and got 8 — a fifteen-day-old item being reported as eight days old, on the one day a
correct queue-age count matters most (the day a parallel superseding entry gets filed
explicicitly because the original is now "stale"). The correct number today is **15
days** (June 12 to June 27). The same wrong "8 days since June 19" framing is repeated
verbatim in `overlay.json`'s `os-rem-switch-inputs-jun20` reminder ("now stale-dated, 8
days old since its own createdAt" — that phrase asserts a recompute from `createdAt` that
demonstrably did not happen, since `createdAt` is June 12). This is not catastrophic — it
doesn't change the date-move decision, the package is correctly identified as stale
either way — but it is exactly the kind of small, checkable regression that should not
survive five days of correct precedent, especially on the day a second queue entry exists
specifically because of date-staleness concern. Filing a task for this.

## 4. New queue entry `ap-2026-06-27-switch-upload-replan` — schema and judgment check

**Schema conformance**: matches the `approvals/queue.json` shape exactly — `id`, `type`,
`title`, `summary`, `draftPaths`, `draftedBy`, `createdAt`, `status: "pending"`. No
`resolvedAt`/`result` (correct, unresolved). `draftPaths` identical to the original
entry's five files, as claimed.

**Append-only respected**: I diffed the original `ap-2026-06-19-switch-upload` entry
against its prior state (it's unchanged from what every previous audit recorded — same
`summary` text, still saying "scheduled to go public on 2026-07-18," same `createdAt`,
still `pending`). The new entry was appended after it, not spliced in or replacing it.
Confirmed genuinely append-only.

**Was superseding the right call vs. leave-as-is (the W26 precedent)?** I stress-tested
Hemera's distinction by trying to find the loophole she might have missed, the same way I
tested the Nightshade/Homerun deferral question above.

The W26 case (`t-w26-x-post-queue-summary-default-inversion`): two framings (reply-to-
thread vs. standalone) of the *same posting action*, where the precondition for one
framing (a published W25 marker post) never existed, so the conditional language in the
queue summary already degraded to the same real-world outcome regardless of which framing
you read literally. Approving that item as-worded could not produce a wrong *action*.

Today's case is different in a way that actually matters, not just in degree: the queue
summary's claim is "scheduled to go public on 2026-07-18." If a user reads only that
summary (which the README explicitly designates as "what will happen if approved, one
paragraph" — the authoritative approval-time text) and approves it, the literal directive
is to schedule a July 18 publish. There is no escape hatch here the way there was in W26 —
there's no reading of "go public on 2026-07-18" that quietly degrades to "go public on
2026-07-25" without someone catching the contradiction first. I tried to construct the
practical-equivalence argument anyway (the prompt asks me to): "nothing is approved or
published yet either way, so does it matter?" It's a real point about *today*, but it
doesn't survive past the moment of approval — the whole point of an approval gate is that
once it's actioned, the stated text becomes the operative instruction for the human
executing the manual upload steps in `checklist.md`. At that point the contradiction
between the queue summary's "July 18" and the checklist's now-corrected "July 25" stops
being hypothetical. So: superseding was the right call, and Hemera's distinction between
the two cases is correctly drawn, not just asserted.

**Residual cost, named plainly**: the queue now has 4 pending items where 3 would do, and
that clutter is real, not free. The README's append-only rule means this can't be cleaned
up without ISΛRK's hand on `team-publish`. That's a structural limitation of the schema,
not a process failure today — see the new task below.

## 5. Nyx's claims — independently verified against the actual files

Read all four upload-package files plus the arc doc directly, not taken on her word.

- **`metadata.json`**: `targetReleaseDate` field reads `"2026-07-25"`. Confirmed fixed as
  claimed — this is structured data, the fix is unambiguous and correct.
- **`captions.md`**: both claimed hits confirmed and corrected — the IG/X caption's
  `186 bpm · c minor · july 25` line and the YouTube community post's `new beat out july
  25 — Switch.` opener. The `drafted: 2026-06-12` header correctly left alone (historical
  record, not a live date claim, exactly as she said).
- **`description.md`**: read in full. Zero date references anywhere — confirmed her claim
  that there was nothing to fix. She did not invent a date mention to manufacture the
  appearance of work.
- **`art-prompt.md`**: read in full. Zero date references — pure visual direction.
  Confirmed clean, untouched, correctly so.
- **`switch-prerelease-arc-w25-w28.md`** (one of the flagged out-of-scope files): read in
  full. Her description is accurate, and if anything understated rather than overstated:
  the header states "release date: July 18, 2026" as a live fact (line 5, not struck
  through), the W25–W28 weekly sections contain a dozen-plus embedded example captions
  naming July 18 directly (`07.18`, `drops july 18`, `link drops at [TBD]` paired with
  July-18-anchored countdown logic), and W27's assets-needed table contains the literal
  line she quoted: "July 18 publish time confirmed — responsible: user." This file is a
  `draftPath` on two currently-pending queue entries
  (`ap-2026-06-17-switch-w25-collab-story`, `ap-2026-06-18-switch-w26-x-post`), meaning a
  user reviewing either approval today would click through to source material that still
  asserts July 18 as the live release date. Her decision not to fix this under today's
  narrower handoff and to file it as its own task instead was correct — this is genuinely
  a different scope than a 5-file metadata sweep, and absorbing it silently into today's
  work would have been scope creep dressed as thoroughness.

Nyx's self-reporting today is accurate, appropriately scoped, and not padded. No findings
against her work.

## 6. Schema / consistency sweep

- **tasks.json**: 39 unique task ids (5 new today: `t-switch-target-date-replan-0627`,
  `t-switch-queue-summary-date-staleness`, `t-argus-pipeline-audit-0627`,
  `t-switch-upload-package-date-framing-nyx`, `t-switch-arc-docs-date-staleness-0627`),
  no duplicates. The three same-day self-closures (`t-switch-target-date-replan-0627`,
  `t-switch-queue-summary-date-staleness` by Hemera; `t-switch-upload-package-date-
  framing-nyx` by Nyx) are consistent with the precedent I endorsed on 2026-06-23: each
  was narrowly scoped to work the closing agent owned and had verifiably finished, cited
  against real artifacts, not closed on say-so. No premature status changes found
  elsewhere.
- **queue.json**: append-only respected (verified in §4). 4 items, 3 originals untouched,
  1 new, all `pending`. No agent wrote a status transition.
- **overlay.json**: valid JSON, no id collisions. One new note
  (`os-note-hemera-standup-0627`), reminders refreshed in place reusing existing ids. The
  one factual defect is the queue-age error in `os-rem-switch-inputs-jun20`, covered in
  §3 — a content error, not a schema error.
- **agents.json**: still stamped `2026-06-26T13:00:00Z` — correctly untouched mid-meeting,
  pending Mnemosyne's end-of-meeting refresh. Sequencing, not staleness.
- **library/beats.json**: unchanged, `updatedAt: 2026-06-12T00:00:00Z`, zero beats —
  correct, intake structurally dark.

## 7. Homerun reversibility — independently re-checked

Globbed `team/drafts/uploads/homerun/**` directly: no hits, directory does not exist.
Confirmed `team/inbox/**` is empty beyond `.gitkeep`. `t-homerun-video-decision` is still
`doing`, not `review`. None of the three 2026-06-21 production-start triggers
(render/export file, committed edit-decision artifact, or a doing→review move with a
linked deliverable) have fired. Reversibility window genuinely open, eighth straight
audit confirming this independently.

## 8. Verdict and new tasks

**Verdict: YELLOW.** Downgrade from three straight GREENs. Not because the big call was
wrong — it wasn't; the Nightshade/Homerun deferral-clause distinction is correctly drawn
and held up under direct stress-testing, the supersession judgment is correctly
distinguished from the W26 precedent, and Nyx's package sweep was accurate and
appropriately scoped. The downgrade is about a smaller thing that matters disproportionately
today: a five-day streak of correct, independently-reproducible queue-age arithmetic broke
on the exact day a second queue entry exists *because* an item's staleness needed to be
taken seriously. "Recomputed from raw `createdAt`" is asserted twice today (calendar,
overlay) for a number that was not actually recomputed from `createdAt` — it was computed
from the item's id-slug date instead, understating the package's real age by a full week
(8 reported vs. 15 actual). On a day when the whole team is asking ISΛRK to trust that
"July 18 is now stale, July 25 is current," a wrong age claim sitting next to that ask is
not cosmetic — it's the kind of small unforced error that erodes the credibility of every
other number in the report. Filing it, not waving it through.

**New task filed:**

`t-queue-age-arithmetic-error-0627` (owner: hemera) — Calendar entry and overlay reminder
both state `ap-2026-06-19-switch-upload` is "8 days [old], since June 19," asserted as
"recomputed from raw `createdAt`." The item's actual `createdAt` is
`2026-06-12T12:30:00Z`; correct age today is 15 days, not 8. This breaks a five-day streak
(06-23 through 06-26 all correctly used June 12 as the anchor and got 11/12/13/14). Fix:
correct both numbers in `q3-2026-release-calendar.md`'s 2026-06-27 log entry and
`overlay.json`'s `os-rem-switch-inputs-jun20` reminder to read 15 days, and going forward,
anchor this item's age to its `createdAt` field, not its id slug, every time — the id date
(June 19) was always the intended approval-gate date, not the creation date, and the two
have diverged since the item was actually drafted June 12.

I'm not filing a second task for the queue clutter (4 pending items, 2 on the same
package) — that's a structural consequence of the README's append-only rule, already
disclosed honestly by Hemera in both the queue entry's own summary and the overlay note,
and there's no agent-side fix available; it's a `user`-decision item already covered by
the existing ask to act via `team-publish`.

## 9. Direct note to ISΛRK

This is the third pre-announced fallback to fire this cycle — Homerun (06-20), Nightshade
(06-22), now Switch (06-27) — and it's the one with a real dollar-and-calendar cost
attached: a dated release moved by a full week, on a track the team called "the anchor
release of Q3," entirely by default, because ten days passed with two pieces of
information (a beat-store URL, a publish time) and three approvals untouched. I checked
the mechanics three ways today and they hold up: the date-move is correctly triggered (not
early, not late, not a convenient misreading of a deferral clause that doesn't exist for
Switch), the cost is honestly computed against the calendar's own numbers (21→14 days
Switch-Homerun spacing, correctly arithmetic-checked), and nothing has been fabricated or
laundered as your sign-off. The team is executing its own stated rules correctly under
real pressure, which is the thing actually worth telling you. The thing not worth
pretending didn't happen: a same-day arithmetic slip (queue age reported as 8 days instead
of 15) on the one artifact whose staleness is the day's headline finding. Small, caught,
fixed by tomorrow — but it's the second time this cycle a number got reported wrong inside
a high-stakes turn (the first was the Nightshade citation error I caught on 06-22), and
two is a pattern worth naming, not a coincidence worth ignoring. The substantive ask is
unchanged and now genuinely urgent in a way it wasn't yesterday: the beat store URL, a
publish time (now against July 25, not July 18), and a decision on which of the two Switch
upload-approval entries to act on. Nothing here needs your decision beyond what was already
pending — but the clock that was "still safe" two weeks ago is the clock that just ran out.
