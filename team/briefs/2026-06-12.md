# Daily brief — 2026-06-12

**TL;DR:** The management team is assembled and the pipeline is built, but it is idle
until you add the Claude token. One test approval is waiting in the queue.

## Needs you
1. **Add the `CLAUDE_CODE_OAUTH_TOKEN` secret** (run `claude setup-token`, paste into
   repo → Settings → Secrets → Actions). Without it no team workflow runs.
2. **Approve or reject** the test item in the queue ("team online" post) to verify the
   round-trip.
3. When ready for intake: `DISCORD_BOT_TOKEN` secret + `DISCORD_INTAKE_CHANNELS`
   variable, and IMAP credentials for hello@isark.beats.

## State of play
- Catalog: Homerun (video pending), Switch (upload scheduled), Virtual Love (needs
  chorus), Nightshade (re-mix starting).
- Task board: 3 open items — two on you (secrets), one on Hemera (Q3 calendar).
- Intake: nothing yet; the sweep runs daily at 07:00 Oslo once credentials exist.

## Next meeting
First real standup runs automatically at 07:30 Europe/Oslo the morning after the
Claude token lands, or trigger it any time: Actions → "Team · Daily Meeting" → Run.
